const arrValue = ['My', 'name', 'is', 'Jack'];

console.log(arrValue);   // ["My", "name", "is", "Jack"]
console.log(...arrValue); // My name is Jack // so you can see it opens up the array and give you directly the element...

//add element to array using spread..

const zee = ['khan','zeehsan',18]

// git init
// git add . (for all files to be commited)
//git add "filename" (for specific file to be commited)
//git commit -m "message" (for commiting the files)
//git log(for seeing the commit history and isse exit karna h toh just press q)
//git log --oneline(for seeing the commit history in a single line)
// commit object-tree object- blob object- file
// HEAD -> pointer to the current branch,,, jo bhi branch mein ho usko head point karta hai.
/* The comment `// inorder to switch to a new branch which is no exists -` seems to be incomplete and
does not provide clear instructions. It appears to be related to branching in Git, but the intended
action or command is missing. */
// inorder to switch to a new branch which is no exists -> git switch -c "branchname"
// git switch "branchname" (to switch to the existing branch)
// git branch (to see the branches)
// git branch -d "branchname" (to delete the branch)
// git branch -D "branchname" (to delete the branch forcefully)
// git branch -m "oldbranchname" "newbranchname" (to rename the branch)
// git branch -m "branchname" (to rename the branch)
// git branch -m "branchname" (to rename the branch)